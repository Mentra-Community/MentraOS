/**
 * `@cloud-v2/proxy` — Forwarder / reverse proxy. OEM-deployable.
 * Handles REST + WS + UDP relay with per-traffic-type upstream config.
 *
 * Spec + design pending: cloud-v2/docs/issues/004-api-proxy/.
 *
 * Currently a placeholder. Real entry point lands as part of subsequent tickets.
 */

import { PACKAGE_NAME as SHARED } from "@cloud-v2/shared";

console.log(`[@cloud-v2/proxy] starting (shared: ${SHARED}). Real implementation pending.`);
