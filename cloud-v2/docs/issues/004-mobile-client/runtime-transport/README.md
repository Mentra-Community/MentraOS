# Runtime Transport

**Status:** Placeholder. Part of the Mobile Client transport (see ../README.md). To be specified.

The v2 client connector that speaks the runtime protocol (WS handshake, REST commands, UDP audio). Wire contract in ../../002-cloud-runtime/protocol.md.
