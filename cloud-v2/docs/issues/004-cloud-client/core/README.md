# Cloud Client: core module (`cloud.core`)

**Status:** Placeholder. Part of the Cloud Client (see ../README.md). To be specified.

Device-facing Cloud Core REST: miniapp bundles + catalog (what `AppRegistry` and the apps store need), and user profile. Stateless typed REST, Bearer from `cloud.auth`. Calls [`../../001-cloud-core/`](../../001-cloud-core/) services (miniapp-service, storage-service). Guardrail: device-facing only, no Dev Console / OEM Portal / store web UI.
