#!/usr/bin/env bun
/**
 * Placeholder. The real pre-flight setup (OS-1486) checks Bun version,
 * Doppler auth, starts the Redis container, and runs Mongo schema migrations.
 *
 * For now this just confirms the script harness works.
 */

console.log("cloud-v2 setup: placeholder. Real implementation lands in OS-1486.");
