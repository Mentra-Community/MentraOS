# Pluggable device OTA implementation

**Goal:** Implement the approved provider architecture for Live, NIMO and AR99; preserve Live policy/recovery; validate the mobile/native paths; open a PR to `dev`, mark it ready for review, and address CI/review findings. Do not merge automatically.

**Spec:** [Device-owned OTA](../specs/2026-09-22-pluggable-device-ota-design.md).

**Branch / PR:** `device-ota-providers`, [PR #4149](https://github.com/Mentra-Community/MentraOS/pull/4149), targeting `dev`. Previous NIMO integration PR #4076 is merged.

## Implemented

- Shared native-device provider registry, explicit pairing/setup requirements, revisioned observation, retained sessions, admission and resource ownership, diagnostics, artifact staging and source policy.
- Live headless session, availability and per-flow auto-chain ownership; existing manifest, transport, timing and presentation behavior. Native journal and connection-context fencing wrap the existing commands. Cold recovery queries status without restoring install/chain approval.
- NIMO Swift/Kotlin protocol, native updater, integrity checks, full firmware/packed/peer compatibility, fresh inventory, reboot verification and conservative interrupted-session recovery. Device differences stay below miniapps.
- AR99 provider and native ownership around the existing transport, preserving vendor requests and reconnect offsets. Image validation remains separate from running-version verification.
- Shared pairing/settings/background/recovery UI, native-identity-preserving Wi-Fi return, scoped hotspot cleanup, runtime/logout admission, and a passive recovery indicator outside signed-in routes.
- Public Engine/SDK observation APIs, OEM composition support and package consumer fixture.

## Rollout and hardware gates

**No phone installs or glasses tests have run for this implementation.** The owner has a Motorola Android phone paired to Live E7FA and explicitly requires permission before any hardware testing. Finish builds/reviews first, then ask. The iPhone is offline. No AR99 is available.

- **Live:** phone regression acceptance remains required. Automated preservation tests do not prove Wi-Fi/hotspot, old firmware, BES/MTK reboot, screen-off or interruption behavior on a physical device.
- **AR99:** the managed replacement is opt-in (`EXPO_PUBLIC_ENABLE_MANAGED_AR99_OTA=true` in the consumer host, or an explicit OEM vendor source). The existing modal/service remains the default. A failed transfer followed by old-version readback does not establish a safe remote abort; tests deliberately retain the unsafe journal and reject reflash. Verify recovery and activation on hardware before removing the old execution path. This is the spec's staged migration gate, not a completed AR99 rollout.
- **NIMO:** the catalogue contains the bench-verified compatibility identity but no production firmware distribution pin. The earlier Mac bench update used the vendor's dirty/Debug image; broad distribution, eligible starting versions and interruption recovery remain unapproved/unverified. That bench device is already on the target and is not an eligible old-firmware phone test. Do not downgrade or reflash it speculatively.

## Validation evidence

Logs are under `.context/ota-implementation/`; earlier research is in `.context/ota-design-review/`, and NIMO protocol/bench evidence is in `.context/nimo-ota-bench/`.

- Full Swift SDK package after merging current `dev`: 200 tests pass, including Live journal/context and NIMO reconnect inventory. Extended AR99 timeout/old-readback/restart suite: six tests pass.
- Full Android SDK compile and unit suite: 466 tests across 74 suites pass. Extended AR99 recovery tests also pass.
- Mobile OTA regression run: 198 tests across 14 suites pass. Follow-up identity/navigation/recovery UI run: 52 tests across seven suites pass; additional Live dismissed-host and AR99 configuration cases pass.
- Full Engine sweep: 1,100 tests pass with no failures, followed by ten passing script/package tests. This includes all 16 Live hook tests, NIMO safe-exit coverage and the synthetic fourth-provider UI. The later provider-open failure close test also passes.
- Engine build and mobile typechecking pass after rebuilding the public declaration output. Changed-file lint retains existing errors in the unrelated `focusEffectPreventBack` hook; new code's lint errors are fixed.
- PR checkpoint CI: Android app build, Swift package, Android tests, quality, public boundary, lockfile and release-family checks pass. iOS app build reached successful build steps and was uploading its post-job cache at the latest inspection. Revalidate the final pushed head.
- Manual Maestro firmware-check flow exists but has **not** been run.

## Review and remaining work

- First independent Codex review requested changes for idle NIMO reconnect inventory, completion navigation blocked by the screen lock, and AR99 uncertain-transfer recovery. The first two are fixed with native/UI regression tests; AR99 rollout is gated and the existing flow restored.
- Live custom presentation now preserves the host's selected native target and entry point. Late completion callbacks do not navigate an unmounted host.
- Second independent review and Bugbot identified explicit recovery reconnect rejection and mismatched native progress units. Both platforms now delegate same-device recovery reconnects to the retained SGC and use fractional progress. Tests verify that a different native identity is rejected without replacing the owner.
- The second review also found the NIMO setup lockout and broken hook test initialization. Safe setup cancellation now returns to device selection without onboarding success; missing-source, unknown-version, failed-check and unsafe-transfer cases have provider coverage plus actual host navigation coverage.
- Native follow-up: 13 Swift recovery/DeviceManager tests pass; Android SDK compile and targeted Live/NIMO/DeviceManager tests pass. The new Live observation record survives a second phone restart without restoring Start approval.
- [x] Finish public package, build and type checks after the setup-result API addition.
- GitHub Codex also found that an initial provider-open failure had Retry but no exit. The generic view now releases a safely idle failed provider and closes, preserving cancelled setup as a distinct outcome; three generic UI tests pass.
- Third independent review found that Live's established BES reboot and legacy APK version completion did not release the new native journal, and that NIMO recovery preparation retries could retain the old manager connection generation. Both are fixed. Live binds the existing coordinator verdict to the native device/updater/session/generation/revision, persists completion before releasing ownership, and keeps the flow retained until native confirms it. Unsupported providers reject host completion evidence. NIMO rebinds the manager only after the current channel is prepared; stale-generation replies cannot complete recovery.
- Follow-up validation: 95 mobile coordinator tests (including actual BES/APK completion), five Engine completion-fence tests, 21 Swift updater tests, and Android SDK compile with the new Live/NIMO recovery tests pass. Full Engine suite and ten package/script tests pass. Public completion evidence is exported through the SDK/Engine facade.
- The third review posted findings but its receipt verification failed because the branch advanced during the run. It is not counted as a final-head review; keep the next review head fixed.
- iOS checkpoint CI failed on artifact-storage network timeouts before compilation; the failed job has been rerun. Android app and other checkpoint checks passed.
- Merged `dev` at `c08c2fa07a`, resolving the old hook versus provider conflict by moving the new version-info failure copy/state into the Live session/presentation. All 16 Live hook tests, including the incoming assertion, pass. Engine build, mobile types and public consumer/boundary checks pass. No `routine:day1-ota` label is enabled; automated hardware testing remains unrequested.
- Fourth independent review completed on `5dd8e7f` with three findings: a failed Live completion handoff had no retry; iOS NIMO cold recovery discovery still required a pairing name; Android NIMO abort retained the previous link's OTA subscription state. All three are fixed. Completion retries re-read and fence the same native transaction (three attempts for revision races), then expose a recoverable error whose Retry only repeats completion. Recovery discovery uses the retained UUID; ordinary pairing retains its name policy. Shared Android link teardown clears OTA characteristics/subscription state.
- Fourth-review regression evidence: 114 Live session/provider/coordinator tests, six completion-fence tests, 16 public Live hook tests, six Swift compatibility/discovery tests, Android SDK compile and a real SGC/CCCD resubscription test pass. The latter rejects old-GATT acknowledgments and waits for the new subscription acknowledgment. Engine build, mobile types and changed-file lint pass.
- Current-head CI at this checkpoint passed all checks except the queued iOS app build/gate. Bigbob is reachable with three active workers; there is no identified user permission dialog. Hardware testing remains disabled.
- All CI checks, including the signed iOS build/publication and Android APK, passed at `34cc29b212`. The fifth independent run stalled once and restarted automatically. Its follow-up found that a phone-side BES timeout could replace the required reboot instruction with completion retry. The coordinator now publishes release safety; unsafe BES failures retain their restart instruction and refuse Done until the existing reconnect/status path proves safety. Completed-pass verification retries remain available.
- Additional AR99 audit: legacy start/end now publish native ownership into the shared snapshot, including across connection changes, so JS blocks logout/device replacement before destructive settings changes. Existing explicit legacy retry/cancel remains admitted; the wire flow and managed rollout gate are unchanged.
- Follow-up validation: 115 Live session/provider/coordinator tests, 13 shared-service tests, 16 Live hook tests, seven Swift AR99 updater tests, and Android SDK compile/AR99 tests pass. Engine build/mobile types and changed-file lint pass; the complete Engine sweep also passes.
- Sixth independent review completed on `007a532` and reproduced a legacy AR99 provider release gap: native returned to safe idle, but the retained provider stayed unsafe. The provider now consumes that transition, releases its runtime lease, clears the stale offer and exposes Check/Close without claiming successful activation. Twelve AR99 provider tests pass, including provider/service integration for legacy completion, cancellation/error, reconnect and reopening.
- The stock Live failure screen now respects both `canRetry` and `canFinish`; unsafe BES recovery has no inert Done button. A renderer test covers restart guidance, safe Done and completion Retry. Engine build, mobile typechecking and changed-file lint pass for this follow-up.
- Seventh independent review completed on `efa793c`: a transient Live native observation error remained latched, and the old mobile watchdog assertion still expected unsafe Done. Fresh identity-validated reads/events now recover the observation without changing the bound transaction; a missed terminal event updates release safety. Twelve completion tests cover initial/refresh failures, healthy recovery and identity rejection. The mobile watchdog regression covers unsafe timeout followed by authoritative terminal status; all 31 progress-screen tests and the full mobile Jest suite (138 suites, 1,203 tests) pass locally.
- Eighth review reproduced uncertain Start admission remaining locked in NIMO/managed AR99 after native returned safe idle. Both providers now consume authoritative idle on events or reopening, including unchanged-revision rereads, and expose a read-only recovery action while the result is uncertain. Six provider/service regressions cover event, reopen and failed-then-successful recovery reads; no second Start, retained files while uncertain, and NIMO setup cancellation without compatibility bypass.
- Bugbot and the eighth review also found that Live's redundant completion refresh could fail after safety was already published. Direct read evidence now enters observation through the same identity/revision/generation checks without a second read. Tests cover initially unavailable observation, terminal evidence and late/wrong-identity rejection. Fifty focused recovery tests, the full mobile suite, Engine build and lint pass; the preceding full Engine sweep passed 1,116 tests plus ten script/package tests.
- Ninth review completed on `9efb2ac` and found two additional boundaries. NIMO/AR99 failed-Start reconciliation now requires an accepted ordered read before releasing ownership; delayed idle replies cannot overwrite newer admission or free its file/runtime. Both race regressions pass. Live's specialized failed-open UI now shares guarded release with the generic host, exposes Close, and reports pairing cancellation separately from success. Its 21 hook tests, two stock-renderer tests, three generic-host tests and 14 service tests pass, including retained-native ownership rejection and late-close fencing.
- Tenth independent review completed on `efee1a8`: the legacy Live facade rejected an existing managed controller but did not reserve execution when legacy attached first. Both entry points now reserve the same controller before preparation/attachment; unsafe legacy unmount retains ownership, and safe terminal cleanup plus detach releases it. Legacy attachment pins the native device asynchronously and binds the same native completion evidence before Start/Finish. Tests exercise both acquisition orders, pending checks, unsafe detach and real BES/APK completion through both owner types.
- Failed-Start reconciliation also exposes read-only recovery when a rejected stale read predates the pre-Start snapshot; neither NIMO nor managed AR99 is left with an unsafe state and no recovery action. Both regressions pass.
- Tenth-review validation: the complete Engine suite and ten script/package tests pass, as do all 138 mobile suites (1,205 tests), Engine build, mobile types and changed-file lint (existing import warnings only). Hardware remains untouched.
- The eleventh independent review on `86b31a3` reproduced the same two Bugbot legacy preparation failures: rejected transport preparation retained an unattached owner, and failed/pending identity pinning prevented host cleanup. Retention now starts only after successful synchronous prepare/attach. Before native binding, cleanup cancels the phone-side request and fences its delayed continuations; native journal safety is not reinterpreted. Five real facade/coordinator regressions cover missing/rejected/pending identity, failed prepare and partially failed attachment. Existing unsafe native ownership tests remain passing.
- Follow-up validation: 1,133 Engine tests plus ten package/script tests, 138 mobile suites with 1,210 tests, 113 focused Live coordinator/provider tests, Engine build, mobile types and changed-file lint pass.
- Twelfth independent review on `a54fac6` and Bugbot reproduced a missing prepare → failed attach sequence. Cleanup now uses the actual unbound native-preparation boundary regardless of a preceding successful prepare. Both activation sequences prove no delayed Start and successful reacquisition. A real native-admitted case proves that later preparation failure/unmount still retains ownership. All 115 focused Live coordinator/provider tests, four legacy facade tests, Engine build, mobile types and changed-file lint pass.
- Thirteenth independent review on `d64f07c` reproduced a suspended Live cleanup race: the session could dispose before the coordinator saw the terminal event, leaving hotspot resources retained. Release now requires completed cleanup as well as native safety. Suspended completion waits for the event dispatch and existing coordinator/native proof, then awaits transport teardown before disposal; failures remain recoverable by reopening. Legacy BES completion keeps its reboot proof attached through that handoff. Old BLE progress cannot recreate ownership after a controller has detached.
- Three real provider/session/coordinator/hotspot regressions cover delayed server shutdown, failed completion read plus explicit recovery, and legacy BES reboot proof. They verify file/network/hotspot/runtime retention and release without a new Start or suspended check. Another provider test verifies reopening waits for cleanup without restoring approval. Existing UI fixtures now finish their simulated native lifetimes before release.
- Follow-up validation: the full Engine suite plus ten package tests, all 139 mobile suites (1,215 tests before the additional passing reopening case), all 21 public Live hook tests, Engine build, mobile types, public consumer check and changed-file lint pass. No hardware actions performed.
- [x] Push the thirteenth-review fix and rerun independent Codex review on a fixed head.
- Fourteenth independent review on `5dab090` and Bugbot reproduced two remaining transitions: passive recovery returned to Check without settling cleanup, and stale BLE progress during teardown could revive a verified completion. Recovery now finishes cleanup before offering a new install. The coordinator coalesces Finish, preserves its verified verdict through teardown and blocks Retry/detach until cleanup settles; fresh unsafe native observations still withhold release.
- Native safety-only changes now publish through the Live session even when the visible screen is unchanged. A disposed provider republishes an inactive retained record. An additional open-host regression proves that native activity during teardown cannot detach the coordinator for a new check; explicit recovery repeats verification without another Start.
- Regression coverage runs the real provider/session/coordinator/hotspot stack for idle recovery, late BLE progress and newer native activity, plus the actual legacy facade for concurrent Finish and attempted Retry/detach. All 125 focused Live tests, the complete Engine sweep plus ten package/script tests, and all 139 mobile suites (1,222 tests) pass. Engine build, mobile types and public consumer checks pass; changed-file lint has no errors (one existing import warning). Hardware remains untouched.
- [x] Push the fourteenth-review fixes and rerun independent Codex review on a fixed head.
- Fifteenth independent review on `95bc761` reproduced a legacy unmount being dropped while asynchronous Finish was pending. The facade now remembers deactivation and honors it after verified cleanup; a newer host attachment supersedes the old unmount. The actual coordinator regression no longer depends on a second detach from a vanished host.
- Cleanup-failure injection also proved that a rejected teardown could still report safe release. The coordinator now retains failed completion ownership until Finish succeeds and blocks new Start/retry admission in the meantime. Tests cover single unmount, remount, overlapping completion, rejected cleanup and explicit recovery with one native Start.
- Follow-up validation: 127 focused Live tests, all 139 mobile suites (1,224 tests), the complete Engine sweep plus ten package/script tests, Engine build, mobile types, public consumer checks and changed-file lint pass (existing default-import warnings only). No hardware actions performed.
- [x] Push the fifteenth-review fixes and rerun independent Codex review on a fixed head.
- Sixteenth independent review on `b078cfeba` and Bugbot reproduced the later native-safe edge: successful cleanup could finish while native still withheld release, with no subsequent facade call to flush the remembered unmount. The facade now observes coordinator snapshots only after cleanup succeeds and deactivation remains pending. It rechecks safety after event dispatch, releases automatically when safe, and retires that observer on release or a new attachment. Failed completion still retains ownership.
- Actual facade/coordinator regressions cover native becoming unsafe during cleanup, then safe after Finish resolves, both with an absent host and with a remounted host. All 109 coordinator tests, all 139 mobile suites (1,226 tests), the complete Engine sweep plus ten package/script tests, Engine build, mobile types, public consumer checks and changed-file lint pass (existing default-import warning only). Hardware remains untouched.
- [x] Push the sixteenth-review fix and rerun independent Codex review on a fixed head.
- Seventeenth independent review on `4864c719` and Bugbot reproduced a remount/unmount variant: host attachment erased the facade's successful-cleanup flag. Cleanup completion now belongs to the coordinator's update attempt, independent of host attachment. The facade only manages the requested unmount and observer; new native Start, a fresh coordinator session or failed Finish invalidate cleanup proof.
- Actual coordinator tests cover repeated remount/unmount cycles before late safety and a newly admitted retry that cannot reuse old cleanup. All 111 coordinator tests, all 139 mobile suites (1,228 tests), the complete Engine sweep plus ten package/script tests, Engine build, mobile types, public consumer checks and changed-file lint pass (existing default-import warnings only). No hardware actions performed.
- Current-head CI's mobile quality job failed before tests on a Bun cache-copy `ENOENT` for `expo-camera`; the failed job was rerun. Android build/native tests and Swift checks passed; iOS was building at this checkpoint.
- [x] Push the seventeenth-review fix and rerun independent Codex review on a fixed head.
- Merged `origin/dev` (`0187e22639`) as `9627916df4`; resolved the device-settings conflict by preserving Live settings and provider-based OTA/legacy AR99 routing. After the merge, all 1,149 Engine tests plus ten script tests, 1,229 mobile tests, Engine build, mobile types, public consumer checks, and 203 Swift tests passed. Android CI's corrupt NDK archive failed before tests; the failed job passed on rerun.
- The eighteenth review timed out without posting a verdict. The nineteenth review of `9627916df4` reviewed the full diff and passed 125 Engine and 227 mobile checks, finding only the Android single-setting bridge's missing identity guard as blocking. Single and batch setting writes now share one normalized, ownership-checked boundary; bridge tests exercise identity rejection, ordinary settings, aliases, and writes after release. The public-mode Android SDK build and full JVM suite pass, including the actual Expo bridge entry points.
- NIMO now persists explicit evidence when an admitted attempt has not reached upgrade entry. That evidence is atomically replaced before ENTER may be submitted. Cold startup can release an attempt stopped during preparation/inspection, without retrying, resuming or resetting the device. Swift/Kotlin regressions cover preparation, each preflight boundary, post-ENTER uncertainty, and older journals without proof; the Swift regression failed on the previous implementation and the fixed suite passes 205 tests. Unknown write outcomes still require vendor recovery. The review accepted this documented rollout limitation; AR99 remains opt-in and NIMO has no production source pin.
- The twentieth independent review accepted the native bridge/pre-entry fixes but reproduced a separate legacy Live clock-recovery gap: the raw SDK restart bypassed the coordinator and left completion bound to the previous native attempt. Clock recovery now routes through either execution owner, preserving the selected Wi-Fi/hotspot transport and rebinding completion through the existing retry path. The shared helper is named `recoverLiveClock`; unowned SDK recovery, eligibility, coalescing and cooldown remain unchanged.
- Four actual-facade regressions cover clock recovery followed by BES reboot or APK build-increase completion over both Wi-Fi and hotspot. All four failed before the fix and pass afterward, including expected native session evidence, release, and unchanged selected URLs. The 142 focused tests and full mobile suite (139 suites, 1,233 tests), Engine build, public consumer check, and mobile types pass. No hardware testing performed.
- Merged `origin/dev` (`cebc63d1a7`) after the clock-retry correction. Preserved the upstream single-in-flight update-check fix in the extracted Live session and retained its hook regression (three calls before, one after). Full Engine/mobile suites, Engine build, public consumer check, mobile types and changed-code lint pass; mobile now has 140 passing suites / 1,234 tests. The previous pushed head `0be66be44d` completed all CI, including signed iOS publication. No device testing performed.
- The twenty-first review passed 126 Engine, 246 mobile and six package checks and accepted earlier fixes, but reproduced Live ownership release on idle after ACK. Swift/Kotlin now retain admitted work through ambiguous idle replies, including cold recovery, until terminal status or verified legacy completion. ACK/query/idle/active/completion and lost-ACK cold-recovery regressions failed before the correction; all 206 Swift, 471 Android and 142 focused Live tests now pass, as does the public-mode Android build. Unowned idle remains safe; unresolved idle-only recovery remains retained.
- [x] Mark PR #4149 ready for review (not hardware-accepted or ready to merge).
- The twenty-second review found lost-terminal Live recovery and authentication teardown gaps. Native recovery now uses ASG's existing correlated activity diagnostics to prove a quiet worker; stale, busy or incomplete replies cannot release ownership. Profile admits logout before closing Settings, and host/headless auth loss suspends optional work without cancelling native recovery. Regression tests cover both native platforms, actual Profile/AuthProvider/headless callbacks and an independently runnable pairing fixture. Validation passes 208 Swift and 473 Android tests, the Engine sweep and ten package checks, 1,240 mobile tests across the full sweep plus the headless callback suite, Engine build/public consumer checks, types and changed-file lint (existing warnings only). No hardware actions performed.
- [ ] Monitor final CI and review bots and resolve supported findings.
- The twenty-third independent review accepted the prior fixes and agreed with Bugbot that host-only workspace auth changes could leave discovery suspended. Both host and Engine auth callbacks now use explicit discovery resumption, which never resumes old update approval; the monitor preserves the mounted host's home context and initial auth reads cannot overwrite newer events. Two monitor regressions failed before the fix. The 1,150 Engine tests plus ten package checks, 1,242-test mobile sweep plus the two added monitor regressions, 21 focused lifecycle checks, types/build/public-consumer checks and changed-file lint pass. Hardware remains untouched.
- [ ] Request permission before Live phone testing and record actual hardware acceptance. Keep unavailable AR99/iPhone and NIMO release gates explicit.
- Merged `origin/dev` (`a641522a2e`) after G2 recovery introduced a device-manager conflict. Disconnect/forget retain OTA admission before clearing G2 recovery; both behaviors are preserved. The just-started twenty-fourth review was cancelled without a verdict so the merged head can be reviewed. Validation passes 479 Android, 1,155 Engine plus ten package checks, and 1,262 mobile tests; Engine/public consumer builds and mobile types pass after regenerating the updated Miniapp SDK definitions. No hardware actions performed.

This is not a production firmware release or a claim of regression-free hardware behavior. The architecture can be reviewed before hardware acceptance; merging/enabling unvalidated device migrations remains a separate gate.
