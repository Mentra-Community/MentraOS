name: Recovery Agent Build

on:
  pull_request:
    paths:
      - "asg_client/recovery_agent/**"
      - ".github/workflows/recovery-agent-build.yml"
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          java-version: "17"
          distribution: "temurin"

      - name: Setup Android SDK
        uses: android-actions/setup-android@v2

      - name: Build recovery agent debug APK
        working-directory: ./asg_client/recovery_agent
        run: ./gradlew clean assembleDebug

      - name: Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: recovery-agent-debug
          path: asg_client/recovery_agent/app/build/outputs/apk/debug/app-debug.apk
