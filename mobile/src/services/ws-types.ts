// WebSocketStatus moved into @mentra/engine (with the connection store that uses it).
// Re-exported here so existing `@/services/ws-types` importers stay unchanged.
export {WebSocketStatus} from "@mentra/engine"
