# Unit tests own their dependencies. Starting AsgClientApplication for each test
# installs a process-global crash handler and repeatedly starts reporting workers,
# allowing background work from one sandbox to terminate later tests.
# Tests of application startup must explicitly opt into AsgClientApplication.
application=android.app.Application
